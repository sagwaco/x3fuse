/**
 * Translation entry point for the renderer. Backed by i18next (see i18n/config),
 * which loads the shared locale bundles. Components keep importing `t` from here.
 */
export { t } from '../i18n/config'
